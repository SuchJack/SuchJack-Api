spring:
  application:
    name: api-backend
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://117.72.79.165:3306/api_database
    username: suchjackroot
    password: root123
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  # session 失效时间（分钟）
  session:
    timeout: 60
    store-type: none
  # redis 配置
#  redis:
#    port: 6379
#    host: 117.72.79.165
#    database: 1
#    password: 123456
server:
  port: 7589
  servlet:
    context-path: /api
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
such:
  client:
    secret-key: jack
    access-key: abcdefgh
#
#dubbo:
#  application:
#  # 设置应用名称
#    name: dubbo-springboot-demo-provider
#  # 指定使用 Dubbo 协议，且端口设置为 -1，表示随机分配可用端口
#  protocol:
#    name: dubbo
#    port: 22221
#  registry:
#    # 配置注册中心为 Nacos，使用的地址是 nacos://localhost:8848
#    id: nacos-registry
#    address: nacos://localhost:8848

dubbo:
  application:
    name: api-backend
    logger: slf4j
#    qos-port: 33333
    qos-enable: false
  protocol:
    name: tri
    port: 50052
  registry:
    address: nacos://${nacos.address:117.72.79.165}:8848
