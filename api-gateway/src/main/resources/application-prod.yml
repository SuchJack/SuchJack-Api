server:
  port: 8090
spring:
  cloud:
    gateway:
      default-filters:
        - AddResponseHeader=source, jack
      routes:
        - id: api_route
          uri: http://localhost:8123
          predicates:
            - Path=/api/**
  application:
    name: gateway
#        - id: add_request_header_route
#          uri: http://localhost:8123
#          predicates:
#            - Path=/api/**
#          filters:
#            - AddRequestHeader=yupi, swag
#            - AddRequestParameter=name, dog
#            - name: CircuitBreaker
#              args:
#                name: myCircuitBreaker
#                fallbackUri: forward:/fallback
#        - id: yupi-fallback
#          uri: https://yupi.icu
#          predicates:
#            - Path=/fallback
logging:
  level:
    org:
      springframework:
        cloud:
          gateway: trace

#dubbo:
#  application:
#    name: dubbo-springboot-demo-consumer
#  protocol:
#    name: dubbo
#    port: 22223
#  registry:
#    id: nacos-registry
#    address: nacos://localhost:8848
dubbo:
  application:
    name: api-gateway
    logger: slf4j
    qos-enable: false
  protocol:
    name: tri
    port: 50052
  registry:
    address: nacos://${nacos.address:117.72.79.165}:8848
