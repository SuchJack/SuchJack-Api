server:
  port: 8090
spring:
  cloud:
    gateway:
      default-filters:
        - AddResponseHeader=source, jack
      routes:
        - id: api_route
          uri: http://localhost:8123
          predicates:
            - Path=/api/**
    nacos:
      server-addr: 110.40.64.81:8848
  application:
    name: gateway
#        - id: add_request_header_route
#          uri: http://localhost:8123
#          predicates:
#            - Path=/api/**
#          filters:
#            - AddRequestHeader=yupi, swag
#            - AddRequestParameter=name, dog
#            - name: CircuitBreaker
#              args:
#                name: myCircuitBreaker
#                fallbackUri: forward:/fallback
#        - id: yupi-fallback
#          uri: https://yupi.icu
#          predicates:
#            - Path=/fallback
logging:
  level:
    org:
      springframework:
        cloud:
          gateway: trace

#dubbo:
#  application:
#    name: dubbo-springboot-demo-consumer
#  protocol:
#    name: dubbo
#    port: 22223
#  registry:
#    id: nacos-registry
#    address: nacos://localhost:8848
# dubbo 配置
dubbo:
  application:
    name: api-gateway-consumer
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://110.40.64.81:8848